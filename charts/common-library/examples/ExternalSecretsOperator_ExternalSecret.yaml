ExternalSecret:
  example-aws-parameterstore:
    refreshInterval: 1m
    secretStoreRef:
      kind: ClusterSecretStore
      name: aws-parameterstore
    labels:
      app.kubernetes.io/instance: example
    dataFrom:
    - extract: { key: '/test/example' }