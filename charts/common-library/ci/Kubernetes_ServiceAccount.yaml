# Kind-schedulable fixture for ServiceAccount.
ServiceAccount:
  ci-serviceaccount:
    annotations:
      hobops.io/ci: "true"
    labels:
      app: ci-serviceaccount
    automountServiceAccountToken: false
