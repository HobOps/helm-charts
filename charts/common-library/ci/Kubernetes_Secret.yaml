# Kind-schedulable fixture for Secret (stringData only; live Secrets store data).
Secret:
  ci-secret:
    annotations:
      hobops.io/ci: "true"
    labels:
      app: ci-secret
    stringData:
      APP_TOKEN: ci-token
