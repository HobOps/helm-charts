# Kind fixture for External Secrets ClusterSecretStore (fake provider).
ClusterSecretStore:
  ci-clustersecretstore:
    annotations:
      hobops.io/ci: "true"
    provider:
      fake:
        data:
        - key: /ci/cluster-example
          value: kind-ci
          version: "1"
