# Kind fixture for External Secrets SecretStore (fake provider; no backend needed).
SecretStore:
  ci-secretstore:
    annotations:
      hobops.io/ci: "true"
    provider:
      fake:
        data:
        - key: /ci/example
          value: kind-ci
          version: "1"
